# Benchmark Environment

Placeholder. Phase 3 fills this in with the actual measurement environment:
CPU model, core/cache topology, SMT state, governor settings, pinning
scripts, build flags, and known sources of run-to-run variance on a
consumer laptop (thermal throttling, no isolated cores). Every number in
`bench/` results is meaningless without this document alongside it.
